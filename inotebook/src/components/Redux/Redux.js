import React from 'react'

const Redux = () => {
  return (
    <div className='container'>
        <h1>What is REDUX</h1>
        redux is stateMangement libreary present in react 
        1. it is advance then props,and noteContext
        2.when the projecat is very large then redux is used other wise not used 
      3. redux provide a global redux shop from where all data can be acess and used in difernt component
    </div>
  )
}

export default Redux
